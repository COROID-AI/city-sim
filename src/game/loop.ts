/**
 * Game loop module (placeholder).
 *
 * The full loop — requestAnimationFrame ticking, board updates, rendering,
 * and input handling — is implemented in a later phase. This module exists so
 * the entry chain (src/main.ts -> src/game/loop) resolves and the Vite dev
 * server boots.
 */
export {};