/**
 * Game loop module.
 *
 * The real loop — ticking the board on a timer, applying arrow-key input,
 * and rendering to the canvas — is implemented in a later phase. This stub
 * exists so the Vite entry point (`src/main.ts`) has a valid module to
 * import and the dev server boots cleanly.
 */
export function startGame(): void {
  // No-op placeholder.
}