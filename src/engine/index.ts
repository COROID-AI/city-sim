/**
 * Engine module barrel.
 *
 * Intentionally empty for now — downstream tasks (GameLoop, GameEngine,
 * Renderer, Camera) will populate this surface. The file exists so the
 * spec-mandated `src/engine/` directory is import-safe from day one.
 */
export {};
