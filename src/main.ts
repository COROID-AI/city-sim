// Vite entry point. The game loop lives in src/game/loop (a minimal stub
// until the game phases land); importing it keeps the dev server's module
// graph wired up so the placeholder page boots cleanly.
import './game/loop';