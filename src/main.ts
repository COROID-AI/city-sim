/**
 * Application entry point.
 *
 * The game loop lives in `src/game/loop.ts` and will be implemented in a
 * later phase. Importing it here keeps the Vite entry chain in place so the
 * dev server boots and serves the placeholder page.
 */
import './game/loop';