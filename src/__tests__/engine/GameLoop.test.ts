/**
 * Smoke test for the engine test directory.
 * Real GameLoop tests will be added by the downstream GameLoop task.
 */
describe('Engine test harness', () => {
  it('should run Jest in the engine test directory', () => {
    expect(true).toBe(true);
  });
});
