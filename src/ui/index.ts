/**
 * UI overlay barrel.
 *
 * Re-exports the five presentational overlay slots so downstream tasks can
 * import them from a single entry point.
 */
export { TopBar } from './TopBar';
export { CityLog } from './CityLog';
export { TimeControls } from './TimeControls';
export { MiniMap } from './MiniMap';
export { Tooltip } from './Tooltip';
