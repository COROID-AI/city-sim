import '@testing-library/jest-dom';

// Ensure jest-dom's matcher type augmentation is visible to TypeScript
// consumers (e.g. ts-jest type-checking and `tsc --noEmit`).
/// <reference types="@testing-library/jest-dom" />
