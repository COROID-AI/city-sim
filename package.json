{
  "name": "tetris-game",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "A web-based Tetris game: Guideline SRS rotation with wall kicks, 7-bag randomizer, hold/next queue, ghost piece, DAS/ARR input, and level-scaled gravity.",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "preview": "vite preview"
  },
  "devDependencies": {
    "typescript": "^5.9.3",
    "vite": "^8.2.1",
    "vitest": "^4.1.10"
  }
}
