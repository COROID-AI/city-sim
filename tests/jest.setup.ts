// (intentionally empty — the real setup lives in tests/jest.setup.js
// so it runs via Jest's `setupFiles` (pre-transform) and the React
// `act` global flag is set BEFORE `react` is required.)
export {};
