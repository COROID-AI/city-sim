import { defineConfig } from 'vite'

// Vanilla TypeScript + Canvas: no framework plugin is required.
// The default Vite config serves index.html at the project root and
// bundles src/main.ts (referenced as a module script from index.html).
export default defineConfig({})